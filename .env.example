# Environment variables for CI Feature Extractor (example)
#
# Note: Chrome extensions cannot read environment variables at runtime.
# These values are provided as a template for local development or for
# future tooling/build steps. Do NOT commit your real secrets.

# OpenAI
OPENAI_API_KEY=
# Optional override (defaults used in code if not provided)
OPENAI_API_URL=https://api.openai.com/v1/chat/completions
DEFAULT_MODEL=gpt-4o-mini

# Supabase
SUPABASE_URL=
SUPABASE_ANON_KEY=

# If you create a build step to inject these into the extension (e.g. generating env.js),
# ensure that you never ship secrets to production and keep .env out of version control.
